<%= form_with(model: [@cliente, @llamado]) do |form| %>
  <% if @llamado.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@llamado.errors.count, "error") %> prohibited this llamado from being saved:</h2>
      <ul>
        <% @llamado.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="form-group">
    <%= form.label :motivo, style: "display: block" %>
    <%= form.select :motivo, ['Requerimiento', 'Reclamo', 'Consulta'], {}, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :fecha, style: "display: block" %>
    <%= form.date_field :fecha, class: "form-control" %>
  </div>
  <br>
  <div class="actions">
    <%= form.submit class: "btn btn-primary" %>
  </div>
<% end %>





